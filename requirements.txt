flask==3.1.2
python-dotenv==1.1.0
# Драйвер PostgreSQL — нужен только при работе с Supabase (DATABASE_URL задана).
# Для локальной разработки на SQLite не требуется.
psycopg2-binary==2.9.10
# Продакшн-WSGI сервер для развёртывания на Render/Railway/Fly.io.
gunicorn==23.0.0
# Правила выбора АБТ (docs/protocols/cap_abt_rules.yaml).
PyYAML==6.0.2
