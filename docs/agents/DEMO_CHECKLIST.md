# Чеклист ручного демо (10 пациентов)

Цель: гость по ссылке видит **один** следующий шаг по протоколу и может пройти контрастные сценарии.

## Среда

- [ ] URL приложения (Render): `https://…onrender.com` *(из панели Render)*
- [ ] Данные в Supabase (после `seed_ten`), не только localhost
- [ ] Каталог лекарств взрослый (КП №768), ≥20 позиций в форме назначений

## Команда сида

```bash
# локально
CLINIC_DB=clinic-qg-run.db python3 tools/seed_ten.py

# облако (Supabase)
DATABASE_URL='postgresql://…' python3 tools/seed_ten.py

# HTTP-аудит всех 10 (сервер уже запущен)
python3 tools/audit_ten.py http://127.0.0.1:5591
python3 tools/live_ui_audit.py http://127.0.0.1:5591
```

`seed_ten` очищает клинические данные, создаёт 10 взрослых с ВП, каталог ЛС и `cap_cache`.
Кнопка на пустом дашборде даёт только Орлов/Б/В — для полного набора всегда `seed_ten`.

## Пациенты (покрытие UI / протокола)

| Пациент | Визиты | Сценарий | Что проверить в UI |
|---|---|---|---|
| **Орлов** Антон | 2 | Эталон амбулаторно | «Соответствует», шаг про контроль R-графии, без формы АБТ |
| **Соколов** Борис | 1 | **Гость:** азитромицин вместо амоксициллина | Headline про АБТ; вверху форма с предвыбранным амоксициллином |
| **Морозов** Виктор | 1 | Тяжёлая амбулаторно, без АБТ | «ОРИТ» / госпитализация; кнопка «Госпитализировать» вверху |
| **Стационаров** Павел | 2 | Стационар OK, к выписке | Соответствует; шаг step-down / контроль |
| **Клавуланова** Мария | 1 | Риск АБТ 3 мес, простой амокс | Нужен амокс/клав |
| **Аллергова** Елена | 1 | IgE на β-лактамы + ошибочный амокс | Макролид |
| **Аспиратов** Игорь | 2 | Аспирация + MRSA, мультирежим | Соответствует; исследования в next_step |
| **Бронхов** Олег | 1 | Бронхолитик без обструкции + короткий курс | Отклонения по АБТ / курсу / бронхолитику (часть в «Ещё») |
| **Контролёв** Андрей | 3 | АБТ без эффекта | Госпитализация; кнопка вверху |
| **Пустова** Наталья | 1 | Почти пустая карта | «Диагноз…»; форма МКБ вверху |

## Сценарий для гостя (2–3 минуты)

1. [ ] Открыть URL / дашборд — guest `page-banner` про Соколова («не та АБТ»).
2. [ ] **Открыть карту** → Соколов: CDS внутри диагноза + «Заменить на Амоксициллин».
3. [ ] Нажать «Заменить…» → вердикт зеленеет (азитромицин снят).
4. [ ] Контраст: **Орлов** (ок) и **Морозов** (ОРИТ/госпитализация).
5. [ ] На дашборде колонки **«Вердикт»** и **«Сделать сейчас»** заполнены у всех.

## Override (осознанное назначение) — зеркало `doctor_gate` §5

1. [ ] Пациент с ВП и ожидаемой АБТ (напр. **Пустова** после постановки диагноза / или чистый кейс).
2. [ ] Назначить **не** first-line АБТ без confirm → диалог / `need_confirm`, препарат **не** сохранён.
3. [ ] «Назначить осознанно» (`confirm=1`) → препарат активен, бейдж **«осознанно»**, вердикт **не** «соответствует».
4. [ ] Смена на препарат по протоколу → override снимается с картины, метрика может стать compliant.

## Автопроверки

```bash
# с нуля (чистая БД → сид → путь врача без вкладок). Если упало — чинить и снова:
python3 tools/doctor_gate.py

python3 tools/scenarios.py          # 24/24
python3 tools/quality_gate.py
python3 tools/audit_ten.py <URL>    # HTTP по уже запущенному серверу
python3 tools/live_ui_audit.py <URL>
```
